# Redis Infrastructure

Redis infrastructure for ULTRONE.

## Use Cases

- **Cache** — Entity state cache, model inference cache
- **Pub/Sub** — Real-time event distribution
- **Queues** — Background task queues (workers)
- **Sessions** — WebSocket session management
- **Rate limiting** — API and safety rate limits

## Quick Start

```bash
docker compose up -d redis
```
