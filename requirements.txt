# ULTRONE Research Platform — Core runtime & test dependencies
# Used by CI (research-platform-ci.yml) and the Docker image (Dockerfile.research).

# ── Core scientific computing ──────────────────────────────────────────────
numpy>=1.24
scipy>=1.10

# ── Machine learning / deep learning ───────────────────────────────────────
torch>=2.1
torchvision>=0.16
gymnasium>=0.29
stable-baselines3>=2.2

# ── Graph / reasoning ──────────────────────────────────────────────────────
networkx>=3.1

# ── Web / API ──────────────────────────────────────────────────────────────
# starlette pinned: fastapi 0.109 passes the removed `on_startup` kwarg to
# Router.__init__ on starlette>=0.37. httpx pinned: starlette 0.36 TestClient
# passes `app=` to httpx.Client, removed in httpx>=0.28.
fastapi>=0.104,<0.110
starlette>=0.36.3,<0.37
httpx>=0.27,<0.28
uvicorn[standard]>=0.24
pydantic>=2.5

# ── AI / perception (module-level imports used by the test suite) ──────────
transformers>=4.36
Pillow>=10.0
librosa>=0.10
matplotlib>=3.8

# ── Observability / infra clients (module-level imports) ───────────────────
psutil>=5.9
redis>=5.0
prometheus-client>=0.19

# ── HTTP / utilities ───────────────────────────────────────────────────────
requests>=2.31

# ── Auth / memory service (module-level imports in apps/ and vendored ultron) ─
PyJWT>=2.8
bcrypt>=4.0
modelscope-hub>=0.4
tiktoken>=0.5

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1

